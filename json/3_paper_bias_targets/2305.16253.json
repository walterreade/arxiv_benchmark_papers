{
  "filename": "2305.16253.pdf",
  "title": "Uncovering and Categorizing Social Biases in Text-to-SQL",
  "arxiv_url": "https://arxiv.org/pdf/2305.16253",
  "bias_targets": [
    {
      "target": "Racial/Ethnicity bias",
      "methodology": "Injecting judgmental modifiers into natural language queries and measuring the frequency with which the Text-to-SQL model inappropriately inserts ethnic demographic filters into the generated SQL query."
    },
    {
      "target": "Religious bias",
      "methodology": "Injecting judgmental modifiers into natural language queries and measuring the frequency with which the Text-to-SQL model inappropriately inserts religious demographic filters into the generated SQL query."
    },
    {
      "target": "Gender bias",
      "methodology": "Injecting judgmental modifiers into natural language queries and measuring the frequency with which the Text-to-SQL model inappropriately inserts gender demographic filters into the generated SQL query."
    },
    {
      "target": "Sexual orientation bias",
      "methodology": "Injecting judgmental modifiers into natural language queries and measuring the frequency with which the Text-to-SQL model inappropriately inserts sexual orientation demographic filters into the generated SQL query."
    },
    {
      "target": "Disability bias",
      "methodology": "Injecting judgmental modifiers into natural language queries and measuring the frequency with which the Text-to-SQL model inappropriately inserts disability demographic filters into the generated SQL query."
    },
    {
      "target": "Age bias",
      "methodology": "Injecting judgmental modifiers into natural language queries and measuring the frequency with which the Text-to-SQL model inappropriately inserts age demographic filters into the generated SQL query."
    },
    {
      "target": "Political bias",
      "methodology": "Injecting judgmental modifiers into natural language queries and measuring the frequency with which the Text-to-SQL model inappropriately inserts political demographic filters into the generated SQL query."
    }
  ],
  "primary_bias_target": "No Primary Bias Target"
}